const allowedOrigins = [
  'http://localhost:4200',
  'http://127.0.0.1:4200',
  'http://ssmodelhighschool.org',
  // ⚠️ TEMP: no SSL yet. Once you get a cert, replace the line above with:
  // 'https://ssmodelhighschool.org'
];

module.exports = allowedOrigins;